# 查看当前使用的 shell
```shell
$SHELL --version
```

# 加载和执行
```shell
source ~/.zshrc
```

# 配置环境变量

```shell
vim ~/.zshrc 
```
>export variable_name=varibale_value
>export variable_name=variable_value
```shell
source ~/.zshrc #重载配置文件
```