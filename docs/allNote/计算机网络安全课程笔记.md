注意：本文大量使用了 AI 进行答案生成，距离期末仅剩下两天（危）
# 题型
选择 
判断
填空 
简答
计算
# 计算机网络安全的概念是什么？网络安全有哪几个特征？各特征的含义是什么
计算机网络安全是指在计算机网络中保护网络系统和通信数据免受未经授权的访问、损坏、篡改或泄露的一系列措施和技术。

网络安全具有以下几个特征：

1. 保密性（Confidentiality）：保密性是指确保数据只能被授权的个体或实体访问和查看。

2. 完整性（Integrity）：完整性是指确保数据在传输过程中不被篡改、修改或损坏。

3. 可用性（Availability）：可用性是指网络系统和资源能够在需要时持续正常运行和可访问。

4. 可信度（Authenticity）：可信度是指确认通信方的身份和确保其真实性。

5. 不可抵赖性（Non-Repudiation）：不可抵赖性是指确保通信双方无法否认其参与过的通信行为。

# 攻击类型
主动攻击包括对数据流进行篡改或伪造数据流，可分为四类：伪装、重放、消息篡改和拒绝服务。

被动攻击采取的方法是对传输中的信息进行窃听和监测，主要目标是获得传输的信息。
有两种主要的被动攻击方式：信息收集和流量分析。

# 密码体制的概念及其组成成分。
用于加解密，解决机密性、完整性、可用性、不可否认性、真实性等问题的一个或几个都系统。
P 明文空间
C 密文空间
K 密钥空间
E 加密算法
D 解密算法
# 对称密码算法的基本原理
1. 密钥生成：在对称密码算法中，首先需要生成一个密钥。密钥是一个固定长度的二进制串，其安全性直接影响到加密的强度。
    
2. 加密过程：在加密过程中，明文（原始数据）被<u>分割成固定长度的数据</u>块，每个数据块独立进行加密。加密算法使用密钥对数据块进行转换，生成密文（加密后的数据）。
    
3. 解密过程：解密过程与加密过程相反。使用相同的密钥，对密文进行解密操作，将其转换回明文。

# DES 算法的加密流程
DES（Data Encryption Standard）是一种对称密钥加密算法，其加密流程如下：

1. 初始置换 IP：打乱明文中的排列顺序
2. 16 轮迭代：
	1. 使用 56 位的密钥生成 16 个 48 位的子密钥
	2. IP 分为两半部分，右半部分作为新的左半部分。
	3. 右半部分和扩展函子密钥进行相应的计算.
	4. 经过 S 盒、置换等操作得到最终的 32 位结果
	5. 最终的 32 位结果与左半部分进行异或运算得到新的右半部分
	6. 进行下一轮
3. 初始逆置换 IP<sup>-1</sup> : 使加解密使用同一种算法。
# 对称密码和非对称密码的主要区别

|特点|对称密码|非对称密码|
|---|---|---|
|密钥数量|仅有一个密钥|有一对公钥和私钥|
|密钥分发|密钥需要安全地交换|公钥可以公开分发|
|加密/解密|使用相同的密钥|使用不同的密钥|
|密钥管理|密钥数量较少，易于管理|密钥数量较多，复杂管理|
|速度|加密和解密速度较快|加密和解密速度较慢|
|安全性|密钥长度需要足够长|密钥长度可以较短|
|用途|适合大量数据的加密|适合密钥交换和数字签名|

# RAS 算法
RSA 算法是一种非对称密码算法，其理论基础是基于数论中的两个重要数学问题：**大整数的质因数分解**和**模幂运算**。

RSA 算法的基本流程如下：

1. 密钥生成：
   - 选择两个大素数 p 和 q，计算它们的乘积 n（n = p * q）。
   - 计算 n 的欧拉函数值φ(n)（φ(n) = (p-1) * (q-1)）。
   - 选择一个小于φ(n)且与φ(n)互质的整数 e，作为公钥的指数。
   - 计算与 e 关于模φ(n)的模逆元 d，作为私钥的指数。

2. 加密：
   - 将明文表示为一个整数 M，满足 0 <= M < n。
   - 使用公钥中的指数 e 和模 n，计算密文 C：C = M^e mod n。

3. 解密：
   - 使用私钥中的指数 d 和模 n，计算密文 C 的解密结果 D：D = C^d mod n。

4. 数字签名：
   - 为了生成数字签名，使用私钥对消息进行加密：S = H(M)^d mod n，其中 H(M)是对消息进行哈希操作。
   - 数字签名是 S，将其与消息一起发送。

5. 验证数字签名：
   - 接收到消息和数字签名 S 后，使用公钥对 S 进行解密：S' = S^e mod n。
   - 对接收到的消息进行哈希操作：H'(M) = H(M)。
   - 如果 S'等于 H'(M)，则数字签名验证成功，否则验证失败。

RSA 算法的安全性基于大整数的质因数分解问题，即难以在合理时间内将密钥中的 n 分解为其质因数 p 和 q。只要质因数分解问题是困难的，RSA 算法就可以提供较高的安全性。
实际使用中，RSA 算法的密钥长度越大，破解难度越大，安全性越高。

# 鉴别函数
根据鉴别符的生成方式，鉴别函数可以分为以下几类：

- 以整个消息的密文作为鉴别符
- 消息鉴别码
- 散列函数：用公开的 ƒ 将任意长度的消息计算出固定长度的散列值，这个散列值就是鉴别码。
- ## 哈希函数和挑战应答函数的区别
基于哈希函数的鉴别函数和挑战-应答鉴别函数在原理上有相似之处，因为它们都使用了某种算法对消息进行处理来生成鉴别符。这种相似性可能导致混淆，但它们在实际应用和具体机制上存在一些区别。

基于哈希函数的鉴别函数是使用事先共享的密钥和哈希函数对消息进行处理，生成固定长度的鉴别符。接收方使用相同的密钥和哈希函数对收到的消息进行处理并进行比较。这种方式主要侧重于验证消息的完整性，确保消息在传输过程中没有被篡改。

挑战-应答鉴别函数是通过在鉴别过程中发送和接收挑战和应答来进行身份验证。发送方接收到一个挑战，使用特定算法对挑战进行处理生成应答，并将应答发送给接收方。接收方使用相同算法对挑战和接收到的应答进行处理并进行比较。这种方式主要侧重于验证发送方的身份，确保发送方具有正确的密钥或信息来生成正确的应答。

尽管这两种鉴别函数都使用了某种形式的算法处理消息，但它们的应用场景和目标略有不同。基于哈希函数的鉴别函数更适用于验证消息的完整性和防止篡改，而挑战-应答鉴别函数更适用于验证发送方的身份和防止重放攻击。

# 什么是消息鉴别码 （MAC）？其实现的基本原理是什么
消息鉴别码（Message Authentication Code，MAC）是一种用于验证**消息完整性**和**认证消息来源**的技术。它是通过使用一个密钥和一个特定的算法，对消息进行处理生成一个固定长度的鉴别码。

MAC 的实现基本原理如下：
发送方使用公开函数和密钥生成固定长度的数据块，附加到原始消息中。
接收方利用与发送方共享的密钥，进行鉴别。

# 散列函数 
散列值（又称为哈希值、消息摘要）的函数。散列函数的首要目标是**保证消息的完整性**。
散列函数是密码学中常用的一种算法，它将输入数据转换为**固定长度的输出**，称为散列值。散列函数应具备以下安全特性：
1. 单向性（One-way property）：散列函数应该是单向的，即从散列值难以还原出原始输入数据。
2. 弱抗碰撞性，又称第二原像攻击。即不能找到与给定消息具有相同散列值的另一消息。
3. 强抗碰撞性，又称为抗碰撞攻击，是指对于给定的散列函数 H，寻找两个不同的消息 M 1和 M2，使得 H（M 1）=H（M2）在计算上是不可行的。

# 数字签名
数字签名是一种用于验证数字文档的完整性、认证发送方身份以及防止抵赖的技术。它是基于非对称密钥密码学的原理，结合了哈希函数和公钥加密算法。
特征：
可验证性：信息接收方必须够验证发送方的签名是否真实有效
不可伪造性：除了签名人之外，任何人不能伪造签名人的合法签名
不可否认性：发送方在发送签名的消息后，无法抵赖发送的行为；
数据完整性：数字签名使得发送方能够对消息的完整性进行校对。

基于公钥原理的数字签名
1. 密钥生成：首先，通信方生成一对非对称密钥，包括私钥和公钥。私钥用于签名生成，而公钥用于验证签名。
    
2. 消息摘要：发送方使用哈希函数对要传输的消息进行摘要处理，生成一个固定长度的哈希值。
    
3. 私钥签名：发送方使用自己的私钥**对摘要值进行加密**，生成数字签名。
    
4. 数字签名附加：发送方将数字签名附加到原始消息中，形成签名和消息的组合。
    
5. 传输消息：发送方将包含消息和数字签名的组合发送给接收方。
    
6. 公钥验证：接收方接收到消息和数字签名后，使用发送方的公钥对数字签名进行解密，得到解密后的哈希值。
    
7. 哈希比较：接收方使用相同的哈希函数对接收到的消息进行摘要处理，得到一个哈希值。
    
8. 验证比较：接收方将解密后的哈希值与自己计算得到的哈希值进行比较。如果两个哈希值相同，则说明消息的完整性未被篡改，并且发送方的身份得到了认证。

# 用户认证的主要方法有哪些 ？各自具有什么特点？

单向认证
通信双方只有一方对另一方进行认证。
应答 B 通过网络发送一个挑战，发送 A 回送挑战响应，应答 B 检查响应，然后再进行通信。

双向认证
通信双方互相验证彼此的身份。

# 设计 Kerberos 是为了解决什么问题？
解决分布式网络下用户访问网络资源时的安全问题。

# 在 Kerberos 中，什么是门票？什么是门票授权门票？
# 简述 Kerberos 中用户工作站获得会话密钥和 TGT 的过程以及获得服务授权门票的过程。

# 什么是证书 ？证书的基本功能是什么？
证书，就是一种经过签名的消息，用来确定某个名字和某个公钥的绑定关系。
基本功能：
1. 身份验证：证书用于验证实体的身份。
    
2. 公钥的合法性验证：确保公钥是由CA授权和颁发的，而不是被篡改或伪造的。
    
3. 安全通信：证书在安全通信中起到关键的作用。通过使用证书，通信双方可以相互验证对方的身份和公钥的合法性。这样可以防止中间人攻击和篡改，确保通信的机密性和完整性。
    
4. 数字签名：证书中的签名是由CA使用其私钥对证书信息进行加密生成的。这个数字签名可以确保证书的完整性和不可篡改性。接收者可以使用CA的公钥来验证签名，从而确认证书的有效性。

# 简述 X.509 证书包含的信息。
1. 版本号（Version）：指定证书的格式版本，常见的版本包括 v1、v2和 v3。
    
2. 序列号（Serial Number）：每个证书都有一个唯一的序列号，用于标识证书的唯一性。
    
3. 签名算法标识（Signature Algorithm）：指定用于对证书进行数字签名的算法，如RSA、DSA、ECDSA等。
    
4. 颁发者（Issuer）：指定颁发该证书的实体的标识信息，通常是证书颁发机构（CA）的名称和其他身份信息。
    
5. 有效期（Validity）：指定证书的有效期限，包括证书的起始日期和截止日期。
    
6. 使用者（Subject）：指定拥有该证书的实体的标识信息，可以是个人、组织或设备的名称、电子邮件地址等。
    
7. 公钥信息（Public Key）：包含证书持有者的公钥，用于加密和验证数字签名。
    
8. 扩展信息（Extensions）：包含一些附加的、可选的扩展信息，用于提供额外的证书属性和功能，如密钥用途、主题备用名称、证书策略等。
    
9. 签名值（Signature Value）：使用颁发者的私钥对证书的其他信息进行数字签名得到的值，用于验证证书的完整性和真实性。
# 简述 X .509 双向认证过程。
1. 服务器准备：服务器方需要获取自己的X.509证书，并将其安装到服务器上。证书中包含了服务器的公钥和其他身份信息。
    
2. 客户端准备：客户端方也需要获取自己的X.509证书，并将其安装到客户端上。证书中包含了客户端的公钥和其他身份信息。
    
3. 握手协商：当客户端与服务器建立连接时，双方开始进行握手协商。这个过程中，双方协商选择加密算法、密钥长度等安全参数，并交换支持的证书类型。
    
4. 服务器验证：服务器向客户端发送自己的X.509证书，客户端收到后会验证服务器证书的有效性。客户端会检查证书的签名是否可信、证书是否过期、证书颁发者是否可信等。
    
5. 客户端验证：如果服务器的证书验证通过，客户端会生成一个临时的对称密钥，然后使用服务器的公钥对该密钥进行加密，并发送给服务器。
    
6. 服务器验证：服务器收到客户端发送的加密的对称密钥后，会使用自己的私钥进行解密，得到对称密钥。服务器验证该对称密钥的合法性。
    
7. 通信加密：一旦验证通过，双方将使用协商得到的对称密钥来加密和解密后续的通信数据，确保数据的机密性和完整性。
# 一个完整的 PKI 应用系统包括哪些组成部分？各自具有什么功能？
一个完整的公钥基础设施（PKI）应用系统通常包括以下组成部分，每个部分都有特定的功能和职责：

1. 证书颁发机构（Certificate Authority，CA）：CA 是 PKI 系统的核心组件之一，是数字证书生成、发放的运行实体，是证书撤销列表 CRL 的发布点，在其上常常运行着一个或多个注册机构 RA。
2. 数字证书库（Certificate Repository）：证书库是 CA 颁发证书和撤销证书的集中存放地，可供公众进行开放式查询。证书库还提供了存取 CRL 的方法。
3. 密钥备份及恢复系统。为了避免用户丢失了用于解密数据的密钥，数据将无法被解密，造成合法数据丢失的情况，PKI 提供备份与恢复密钥的机制。
4. 证书作废系统。证书作废系统是 PKI 的一个必备组件，证书在有效期以内也可能需要作废，原因是密钥介质丢失或用户身份变更等。
5. 应用接口。PKI必须提供良好的应用接口系统，使得各种各样的应用能够以安全、一致、可信的方式与其交互，确保安全网络环境的完整性和易用性。

# 简述 CA 的基本职责。
CA 的主要职能如下：

制定并发布本地CA 策略。本地策略只是对上级CA 策略的补充，不能与之相违背。

对下属各成员进行身份认证和鉴别。

发布本CA 的证书或者代替上级CA 发布证书。

产生和管理下属成员的证书。

证实RA的证书申请，返回证书制作的确认信息或者返回己制作的证书。

接收和认证对所签发证书的撤销申请。

产生和发布所签发的证书和 CRL。

保存证书、CRL 信息、审计信息和所制定的策略

# 混合加密
混合加密是一种使用两种或更多加密算法结合在一起的方法，旨在兼顾各种算法的优势，提高整体安全性。典型的混合加密方案结合了对称密钥加密和非对称密钥加密两种技术。

通常，混合加密的过程如下：

1. **使用非对称密钥加密传输对称密钥：** 发送方生成一个随机的对称密钥，并使用接收方的公钥对其进行加密。

2. **通过对称密钥加密进行数据传输：** 使用刚才生成的对称密钥对实际数据进行加密和解密。对称密钥加密算法通常更高效，适合处理大量数据。

3. **确保密钥的保密性：** 由于对称密钥只在通信开始时传输，而后面的数据传输都使用对称密钥加密，因此整个通信过程中非对称密钥只用于传输对称密钥，保证了对称密钥的相对安全性。

这种混合加密的方法兼具非对称密钥加密的安全性和对称密钥加密的高效性，是在互联网通信等场景中常见的加密方式。例如，SSL/TLS 协议就广泛采用混合加密，使用非对称密钥进行身份验证和对称密钥进行加密通信。


# IPSec 提供哪些服务？
IPSec 提供了跨局域网、广域网甚至跨 Internet 进行端到端安全通信的能力，为主
机到主机的通信流提供机密性、完整性、真实性等安全服务。

这些服务使得 IPSec 成为保护 IP 通信的强大工具，广泛应用于虚拟专用网络（VPN）、远程访问、站点到站点连接等场景，以确保数据的安全性、完整性和可信度。


# 什么是 SA ? SA 由哪些参数来表示？


# 传输模式与隧道模式有何区别 ？
传输模式是对 IP 载荷和 IP 包头的选中部分，IPv 6 的扩展报头进行认证；
隧道模式是对证个内部 IP 包和外部 IP 报头的选中部分，外部 IPv 6 的扩展报头进行认
证。

# AH 协议和 ESP 协议各自提供哪些安全服务？
封装安全载荷（ESP）协议为 IP 数据包提供数据完整性校验、身份认证和数据加密，
还有可选择的抗重放攻击保护；即除 AH 提供的所有服务外，ESP 还提供数据保密服务，
包括报文内容保密和流量限制保密。

# SSL 由哪些协议组成？各自完成什么功能？
SSL（Secure Sockets Layer）是一种用于保护网络通信安全的协议。它由以下几个协议组成：

底层：SSL 记录协议。上层：SSL 握手协议、SSL 修改密码规范协议、SSL 警告协议。
SSL 记录协议规定了数据传输格式。
SSL 握手协议使得服务器和客户能够相互认证对方的身份，协商加密和 MAC 算法以及用
来保护 SSL 记录中发送的数据的加密密钥。
SSL 修改密码规范协议：改变密码规范协议用于从一种加密算法转变为另外一种加密算
法。SSL 警告协议：客户机和服务器发现错误时，向对方发送一个警报消息。


# 描述 SSL 协议的基本流程。
SSL（Secure Sockets Layer）协议的基本流程如下：

①客户端向服务器端发送请求，请求建立 SSL 连接。
②服务器向客户端发送证书，证书包含了服务器的公钥以及证书的签发机构等信息。
③客户端验证服务器证书的合法性，包括证书的有效性、签发机构是否可信等。
④如果服务器证书验证通过，客户端生成随机数作为对称密钥，并使用服务器的公钥对
其进行加密，然后发送给服务器。
⑤服务器使用自己的私钥对加密的随机数进行解密，得到对称密钥。
⑥服务器将数据使用对称密钥进行加密，然后发送给客户端。
⑦客户端使用对称密钥对加密的数据进行解密，得到原始数据。
⑧ SSL 连接建立完成后，客户端与服务器端开始正常通信
# 发送时，SSL 记录协议执行哪些操作？分别完成什么功能？

# 简述 SSL 握手协议的流程
SSL（Secure Sockets Layer）握手协议是建立安全连接并协商加密参数的过程。下面是 SSL 握手协议的简要流程：

①客户端向服务器发送 SSL 连接请求。
②服务器将证书和公钥发送回客户端。
⑧客户端验证证书的合法性，并生成一个随机数（称为“预主秘钥”）。
④客户端使用服务器的公钥加密预主秘钥，并将其发送给服务器。
⑤服务器使用自己的私钥解密预主秘钥。
⑥服务器和客户端分别使用预主秘钥进行加密和解密，建立安全通信通道。

# 什么是网络攻击 ？常见的攻击手段有哪些？
一般意义上的网络攻击是指利用网络与计算机系统中存在的漏洞和安全缺陷，
对金业、政府部门或校园等计算机网络的硬件、软件及其维护的数据进行的人侵与破坏。

常见的网络攻击手段包括：

跨站脚本攻 

跨站请求伪造

SQL 注入攻击

拒绝服务攻击
# 简述网络攻击的过程。
1. 信息搜集：攻击者收集目标网络和系统的信息，包括漏洞、网络拓扑、用户信息等。
2. 入侵与破坏：攻击者尝试利用漏洞或弱点进入目标网络或系统，可能通过密码破解、漏洞利用等方式。
3. 控制与维护：攻击者获取对目标系统的控制权，以便进一步操作、窃取信息或进行破坏。并且隐藏留后门等。
4. 痕迹清理：攻击者尽可能隐藏自己的痕迹，以避免被发现和追踪。清理日志等。
# 在整个 DDoS 攻击过程中有哪些角色？分别完成什么功能
1. 攻击者：发起攻击的人或组织，通过控制大量的僵尸主机来向目标服务器发送大量的请求。
2. 攻击傀儡机：受攻击者控制的感染了恶意软件的受害者计算机，通过攻击者的指令进行攻击。
3. 主控傀儡机：攻击者控制的服务器，用于向僵尸主机发送指令和控制其行为。
4. 被攻击者：攻击的目标，通常是一个网站或网络服务，受到大量请求导致服务不可用。
# 简述僵尸网络的工作原理
攻击者传播僵尸程序传染大量主机，并通过一对多的命令与控制信道控制被感染的
主机，搭建一个能发起多种大规模攻击的平台。一个僵尸网络可以控制大量的用户终端，
可以获得强大的分布式计算能力和丰富的信息资源储备

# 什么是防火墙 ？它有哪些功能和局限性？
防火墻是一个由软件和硬件组合而成的、起过滤和封锁作用的计算机或网络系统，
目的是保护本地网络不受外界的非法访问和攻击。

防火墙的主要功能包括：

①访问控制：最基本和最重要的功能，目的是保护本地网络的资源和数据。为此，防火墙定义了单一阻塞点，阻止非授权的通信进入本地网络，并且禁止潜在的、易受攻击的服务进入或离开本地网络。
②内容控制功能。根据数据内容进行控制，例如过滤垃圾邮件、限制外部只能访
问本地 web 服务器的部分功能等。
③日志功能。防火端可以完整地记录网络访问的情况。一旦网络遭到破坏，可以对
日志进行审计和查询，以查明事实。
③集中管理功能。针对不同的网络情况和安全需要，制定不同的安全策略，并在
防火墻上集中实施。防火墻易于管理，便于管理员集中实施安全策略。
④自身安全和可用性。防火墙要保证自己的安全，不被非法侵入，保证正常的工
作。如果防火墻被侵入，安全策略被破坏，则内部网络就变得不安全。
能。
此外，防火端还具有流量控制、网络地址转换（NAT）、虚拟专用网（VPN）等功
局限性：
①防火墙不能防御不经由防火墻的攻击。
②防火墻不能防范来自内部的威胁。
③防火墻不能防止病毒感染的程序和文件进出本地网络。
④防火墻不能防止数据驱动式的攻击。

# 简述包过滤原理。
1. 数据包检查：防火墙检查数据包的源 IP 地址、目标 IP 地址、端口号和协议等信息。
2. 访问控制列表（ACL）：防火墙根据预定义的规则和策定，将数据包与访问控制列表进行比较，以确定是否允许通过。
3. 决策过程：防火墙根据规则中的条件进行决策，如允许、拒绝或进一步检查。
4. 动作执行：根据决策结果，防火墙采取相应的动作，如允许数据包通过或丢弃数据包。

# 状态检测技术具有哪些特点 ？
状态检测技术结合了包过滤技术和代理服务技术的特点，与包过滤技术一样，他对
用户透明，能够在 OSI/RM 网络层上通过 IP 地址和端口号过滤进出的数据包；与代理服
务技术一样的是，可以在 OSl/RM 应用层上检查数据包内容，查看这些内容是否符合安
全规则。
状态检测技术克服了包过滤技术和代理服务技术的局限性，能根据协议、端口及源
地址、目的地址的具体情况决定数据包是否通过。对于每个安全策略允许的请求，状态
检测技术启动相应的进程，可快速地确认符合授权标准的数据包，使得运行速度加快。
状态检测技术的缺点是状态检测可能造成网络连接的某种迟滞，不过运行速度越快，
这个问题就越不易察觉

# 简述屏蔽子网防火墙的结构。
在这种配置中，使用了两个包过滤路由器：一个在堡垒主机和 Internet 之间，称为
外部屏蔽路由器，另一个在堡垒主机和内部网络之间，称为内部屏蔽路由器。一个
路由器都被配置为只和堡垒主机交换流量。外部路由器使用标准过滤来限制对堡垒主机
的外部访问，内部路由器则拒绝不是堡垒主机发起的进入数据包，并且只把外出数据包
发给堡垒主机。

# 什么是入侵检测 ？入侵检测的典型过程是什么？
入侵检测是一种对网络进行实时监控，在发现可疑行为时发出警报，并采取反应措
施的网络安全技术，是一种主动的安全防御措施，已成为搭建网络安全防护体系的重要技术手段之一。
典型过程包括信息收集、信息预处理、数据的监测分析以及根据安全策略作出响应，
有的还包括检测效果的评估。
信息收集是指从网络或系统的关键点得到原始数据，这里的数据包括原始的网络数
据包、系统的审计日志、实际应用程序日记等原始信息。数据预处理是指对收集到的数据进行预处理，将其转换为检测器所需的格式，也包括对冗余信息的去除，即数据简约。
数据的检测分析是指利用各种算法建立检测器模型，并且对输入的数据进行分析以判断
入侵行为的发生与否，入侵检测的效果如何将直接取决于检测算法的好坏。这里所说的
响应是指产生检测报告，通知管理员，断开网络连接或更改防火端的配置等积极的防御措施。入侵检测被视为防火墻之后的第二道防线是，动态安全技术的核心之一

# 入侵检测和防火墙的主要区别
入侵检测系统（IDS）和防火墙在网络安全中扮演不同的角色：

- 防火墙位于网络边界，通过规则和策略管理进出的数据流量，控制访问和保护网络免受未经授权的访问和攻击。
- 入侵检测系统则是在网络内部部署，监控网络流量和系统行为，检测和识别潜在的入侵行为和恶意活动。

二者的关系是，防火墙主要用于<u>预防和阻止未经授权的访问和攻击</u>，而入侵检测系统则用于<u>检测和识别已经发生的入侵行为</u>，通过实时监控和分析来提供对网络安全事件的可见性和响应能力。

# IDS 的基本功能
①检测并分析用户和系统的活动。
②核查系统配置与漏洞
③识别已知的攻击行为并报警。
④统计并分析异常行为。
⑤对操作系统进行认知管理，并且识别违反安全策略的用户活动。

# 典型的 IDS 实体
1. 事件生成器
2. 事件发生器
3. 事件数据库
4. 响应单元
5. 目标服务器

# 异常检测和误用检测的基本原理
异常检测，也称为基于行为的检测，亡来源于这样的思想，任何一种入侵行为都能
由其偏离正常或所期望的系统和用户的活动规律而被检测出来。异常检测通常首先从用户的正常或合法活动收集一组数据，这一组数据集被视为“正常调用”。若用户偏离正常调用模式，则被视为入侵。这就是说，任何不符合以往活动规律的行都将被视为入侵行为。
误用检测：又称为特征检测，建立在对过去各种己知网络入侵方法和系统缺陷知识
的积累之上。入侵检测系统中存储着一系列已知的入侵行为描述，当某个系统的调用与一个已知的入侵行为相匹配时，则视为入侵行为。

# 公钥密码认证和加密
公钥密码体制是建立在数学中的**单向陷门函数**的基础之上的。

# 混合加密

# Diffie-Hellman
加密是一种公钥密码体制，由Whitfield Diffie和Martin Hellman于1976年提出。它的目标是解决密钥分发的问题，使得两个通信方可以在公开信道上协商出一个共享的秘密密钥，用于后续的对称加密通信。